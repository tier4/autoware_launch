name: Sync AWF main to awf-latest (via PR)

# TODO: This workflow currently syncs via a PR for backward compatibility.
# Change it to direct mirroring whenever ready.

on:
  schedule:
    - cron: 50 */1 * * * # every 1 hour (**:50)
  workflow_dispatch:

jobs:
  sync-awf-latest:
    runs-on: ubuntu-latest
    # only run on vanilla, not the forked repositories
    if: ${{ github.repository == 'tier4/autoware_launch' }}
    steps:
      - name: Generate token
        id: generate-token
        uses: tibdex/github-app-token@v1
        with:
          app_id: ${{ secrets.APP_ID }}
          private_key: ${{ secrets.PRIVATE_KEY }}

      - name: Run sync-branches
        uses: autowarefoundation/autoware-github-actions/sync-branches@v1
        with:
          token: ${{ steps.generate-token.outputs.token }}
          base-branch: awf-latest
          sync-pr-branch: sync-awf-latest
          sync-target-repository: https://github.com/autowarefoundation/autoware_launch.git
          sync-target-branch: main
          pr-title: "chore: sync awf-latest"
          pr-labels: |
            bot
            run:build-and-test-differential
          auto-merge-method: merge
