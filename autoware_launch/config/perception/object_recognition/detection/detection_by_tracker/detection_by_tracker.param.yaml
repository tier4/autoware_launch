# This file is managed by sync-params workflow.
# Keys marked with '{OVERRIDE}' or '# {OVERRIDE: reason}' are persisted as variant overrides.
# 'reason' can be any single-line text without braces.
# To keep local changes, annotate fields with '# {OVERRIDE}' or '# {OVERRIDE: reason}'.
#
# Source: https://github.com/autowarefoundation/autoware_universe/blob/f28a164f6724d1e93e080bb2a5bfef9d6f75f20d/perception/autoware_detection_by_tracker/config/detection_by_tracker.param.yaml

/**:
  ros__parameters:
    tracker_ignore_label.UNKNOWN : true
    tracker_ignore_label.CAR : false
    tracker_ignore_label.TRUCK : false
    tracker_ignore_label.BUS : false
    tracker_ignore_label.TRAILER : false
    tracker_ignore_label.MOTORCYCLE : false
    tracker_ignore_label.BICYCLE : false
    tracker_ignore_label.PEDESTRIAN : false
    tracker_ignore_label.ANIMAL : false
    tracker_ignore_label.HAZARD : true
